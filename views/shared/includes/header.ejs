<header>
    <div>
        <a href="/">SHOPPING</a>
        <nav>
            <ul>
                <% if(locals.isAuth){ %>
                    <li>
                        <button>Logout</button>
                    </li>
                <% } %>
            </ul>
        </nav>
    </div>
</header>